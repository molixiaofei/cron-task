name: 定时任务名称
on:
  schedule:
    # 使用 Cron 语法，注意是 UTC 时间
    - cron: '0 2 * * *'  # 每天凌晨2点(UTC)执行
  workflow_dispatch: # 强烈建议添加，方便手动测试

jobs:
  your-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 执行你的脚本
        run: |
          # 在这里写你的命令，或运行项目中的脚本
          echo "Hello, world!"
